<resources>

    <!-- Base application theme. -->
    <style name="CustomTheme" parent="android:Theme.Material.Light">
        <!-- Customize your theme here. -->
        <!-- Main theme colors -->
        <!--   your app branding color for the app bar -->
        <!--<item name="android:actionBarTabStyle">@style/ActionBarTabBar</item>-->
        <item name="android:actionBarStyle">@style/MyActionBarStyle</item>
        <item name="android:actionBarTabTextStyle">@style/TabTextStyle</item>
        <item name="android:colorPrimary">@color/primary</item>
        <!--   darker variant for the status bar and contextual app bars -->
        <item name="android:colorPrimaryDark">@color/primary_dark</item>
        <!--   theme UI controls like checkboxes and text fields -->
        <item name="android:colorAccent">@color/accent</item>
    </style>

    <!--<style name="ActionBarTabBar" parent="@android:style/Widget.Holo.Light.ActionBar.TabBar">-->
        <!--<item name="android:paddingLeft">30dp</item>-->
        <!--<item name="android:paddingRight">30dp</item>-->
    <!--</style>-->


    <style name="MyActionBarStyle" parent="@android:style/Widget.Material.Light.ActionBar">
        <item name="android:titleTextStyle">@style/TitleTextStyle</item>
        <item name="android:gravity">center</item>
        <item name="android:background">@color/primary</item>
        <item name="android:backgroundStacked">@color/primary</item>
        <item name="android:backgroundSplit">@color/primary</item>
        <item name="android:textColor">@color/form_color</item>
    </style>

    <style name="TabTextStyle"
        parent="@android:style/Widget.Material.Light.ActionBar.TabText">
        <item name="android:textColor">@color/form_color</item>
    </style>




</resources>
